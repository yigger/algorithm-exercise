language: c
dist: trusty

compiler:
  - gcc

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - libc++-dev
      - cmake-data
      - cmake
      - time
      - cpputest
      - pkg-config
      - gcc-5.4
      - g++-5.4

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make test
